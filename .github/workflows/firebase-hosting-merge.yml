# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build
        env:
          FB_ADMIN_PROJECT_ID: ${{ secrets.FB_ADMIN_PROJECT_ID }}
          FB_ADMIN_CLIENT_EMAIL: ${{ secrets.FB_ADMIN_CLIENT_EMAIL }}
          FB_ADMIN_PRIVATE_KEY: ${{ secrets.FB_ADMIN_PRIVATE_KEY }}
          FB_ADMIN_DATABASE_URL: ${{ secrets.FB_ADMIN_DATABASE_URL }}
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_FNM_JH_IVI }}
          channelId: live
          projectId: fnm-jh-ivi
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
          FB_ADMIN_PROJECT_ID: ${{ secrets.FB_ADMIN_PROJECT_ID }}
          FB_ADMIN_CLIENT_EMAIL: ${{ secrets.FB_ADMIN_CLIENT_EMAIL }}
          FB_ADMIN_PRIVATE_KEY: ${{ secrets.FB_ADMIN_PRIVATE_KEY }}
          FB_ADMIN_DATABASE_URL: ${{ secrets.FB_ADMIN_DATABASE_URL }}
